<h1>Folders</h1>
<ul>
  <% for (const folder of folders) { %>
    <li>
      <%= folder.name %>
      <a href="/folders/<%= folder.id %>">View</a>
      <a href="/folders/<%= folder.id %>/edit">Edit</a>
    </li>
  <% } %>
</ul>
